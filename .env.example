# Copy to .env at the repository root for local development.
# GitHub token with Contents: read access to mondeggo/AutoGlow2.
AUTOGLOW_TOKEN=

# Optional: pin AutoGlow to a full commit SHA; empty uses the default branch.
AUTOGLOW_VERSION=
